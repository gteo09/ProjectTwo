
        <!-- -->        
        <div class="input-field col s4" class="text-center" >
          <select id="search-dropdown">
            <option value="" disabled selected>Choose a search option</option>
            <option value="1">Titles</option>
            <option value="2">Actors</option>
            <option value="3">Years</option>
            <option value="4">Television</option>
          </select>
        </div>
        <!-- -->
        <div class="row">
          <form class="col l9">
            <div action="/api/search"> 
              <div class="form-group input-field col l8">
                <label for="example-text"></label>
                <input type="text" name="input" id="example-text" class="form-control" aria-describedby="example-text" placeholder="Enter search term">
              </div>
              <div class="form-group col l3">
                <button id="search-database" class="btn btn-primary">Submit</button>
              </div>
          </form>
        </div>

        <div class="row selections">
          <h4 id="div-header"></h4>
          <section class="col l12" id="resultscatcher">
            <!-- here -->
            <div id="ex1" class="modal">
              <p>Add to: </p>
              <ul>
                {{#each list_min}}
                  {{> watchlists/popuplists}}
                {{/each}}
              </ul>
            </div>

            <div id="image"></div>
          </section>
          </div>


      <div class="row selections">
          <h4 id="standards-header"></h4>
          <section class="col l12" id="stock-genres"></section>
          
          </div>
        </div>
        <!-- -->



<script src="/js/index.js"></script>
<script>
  $("#create_list").on("click", function(event){
    event.preventDefault();
    $.ajax("/api", {
      type: "POST",
      data: {
        listName: $("#newListNameInput").val(),
        cover_img: "https://image.tmdb.org/t/p/w200/pKGGzotqkNDEL2ix4VU1bWmteK5.jpg"
      }
    }).then(
      function(){
        location.reload(true);
      })
  });

  $(".addtocat").on("click", function(event){
    event.preventDefault();
    $.ajax("/api", {
      type: "GET"
    }).then(

    )

  });
  


</script>